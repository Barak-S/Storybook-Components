import React, { FC } from 'react';
import { FontIconProps } from './types';

export const EyeClosedIcon: FC<FontIconProps> = ({ style }) => {
  return <i style={style} className="lar la-eye-slash"></i>;
};

export default EyeClosedIcon;
