import { StyleProps } from 'styles';

export interface CustomIconProps {
  width?: string | number;
  height?: string | number;
  fill?: string;
  stroke?: string;
  strokeWidth?: string;
  style?: StyleProps;
}

export type FontIconProps = StyleProps;
