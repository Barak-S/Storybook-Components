export interface User {
  firstName: string;
  lastName: string;
  email: string;
}

export interface TeamMemberInvite {
  firstName: string;
  lastName: string;
  companyName: string;
  email: string;
  userGroup: string;
  companyType: string;
  message: string;
}
